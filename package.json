{
  "name": "modwerks-landing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:vercel": "npm run build && node prepare-deploy.mjs",
    "start": "python3 -m http.server 8080"
  }
}
